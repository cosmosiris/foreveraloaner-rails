<%= render 'errors' %>

<div class="post-directions">
	<p class="center">Please follow the guidelines below for creating a post</p>
	<ul class="post-directions-list">
		<li>Title should mention the items you are listing</li>
		<li>Use the description to explain what type of agreement you are looking for and the condition of the items you are listing</li>
		<li>Do not mark your post as closed until someone has booked your items or you are no longer offering your items up for rent</li>
		<li>Your price should be more than just a number. Please format it with a dollar amount and a time period. For example:</li>
		<ul>
			<li>$100 a month</li>
			<li>$10 a day for the entire toolbox</li>
			<li>$5 a day per sleeping bag</li>
		</ul>
		<li>Only share your contact info in the description if you feel comfortable</li>
	</ul>
</div>

<%= form_for @post, url: {action: "update"}, html: {class: "post-form", multipart: true} do |f| %>

	<div>
		<%= f.text_field :title, class: "title-input", placeholder: "Title"%>
	</div>
	<div>
		<%= f.text_area :description, class: "desc-input", placeholder: "Description"%>
	</div>
	<div>
		<%= f.label "Select a Category"%>
		<%= f.select :category_id, options_from_collection_for_select(@categories, 'id', 'name')%>
	</div>
	<div>
		<%= f.label "Price" %>
 		<%= f.text_field :price,  class: "price-input" %>
	</div>
	<div>
		<%= f.label "Zip Code" %>
		<%= f.text_field :zip_code %>
	</div>
	<div>
		<%= f.label "City" %>
		<%= f.text_field :city %>
	</div>
	<div>
		<%= f.label "Status of this post" %>
		<%= f.select :status, %w[open closed] %>
	</div>
	<div>
		<%= f.label "Is the price negotiable?"%>
		<%= f.select :negotiable, [["yes", true], ["no", false]] %>
	</div>
	<div>
		<%= f.label "Image" %>
		<%= f.file_field :image %>
	</div>
  	<%= f.submit "Create" %>
<% end %>

<%= link_to 'Back', post_path(@post) %>